## Что и зачем
<!-- Что меняется и почему. Если есть спека — ссылка: openspec/changes/<id>/ -->

## Запущенные проверки
<!-- Команды и результаты. Для изменений загрузки, памяти или рендера «сборка прошла» — не доказательство. -->
- [ ] `npm run check` (architecture, lint, build)
- [ ] Проверки из раздела «Готово» спеки:
- [ ] Не проверено, и почему:

## Критичные пути
<!-- Вывод `git diff --name-only origin/main...HEAD | node scripts/select-reviewers.cjs`.
     Если задет критичный путь, CI добавит отдельного агентного ревьюера; мёрж — за владельцем из CODEOWNERS. -->

## Изменение harness (AGENTS.md, docs/architecture, .agents/)

Гипотеза: что агенты должны начать делать лучше?

| Кейс              | До (main) | После (этот MR) |
|-------------------|-----------|-----------------|
| add-microfrontend |  ? / 5    |  ? / 5          |
| add-lazy-route    |  ? / 5    |  ? / 5          |

Модель и настройки:
Какие провалы остались:

<!--
Удали этот раздел, если MR не трогает AGENTS.md, docs/architecture или .agents/.
Прогоны: EVAL_REF=origin/main evals/run-case.sh <case> 5, затем EVAL_REF=HEAD evals/run-case.sh <case> 5
(см. evals/README.md). Мёржим, только если «После» лучше и ни один другой кейс не стал хуже.
-->
